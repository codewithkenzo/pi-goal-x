- 2026-05-25: Created stacked paused-resume guard spec after reproducing stale checkpoint cascade in resumed paused-goal sessions. Explore/research mapped likely fix to startup/tree continuation queue gating and before-agent-start reconciliation before checkpoint actionability.
- 2026-05-25: Implemented paused-resume guard. Startup, tree, and compaction paths now queue only actionable focused continuations; session resume no longer prompts/reactivates paused goals; `before_agent_start` reconciles focused goal from disk before checkpoint actionability so persisted paused state stales/aborts old checkpoints. Added regression coverage for paused `session_start`, `session_tree`, disk-reconciled stale checkpoint abort, stale fallback blocking (`bash`, `goal_question`, `subagent`, unknown tool) with safe `get_goal`, and explicit `/goal-resume` active continuation. Verification passed: `node --experimental-strip-types --test tests/goal-extension.test.ts`, `npm test`, `bun test`, `npm run check`, and `git diff --check`.
- 2026-05-25: Ran isolated local extension QA with a temporary session directory via `/tmp/manual-qa-paused-resume.mjs`. Flow: create dummy Sisyphus goal, pause it, emit `session_start` resume and `session_tree`, confirm persisted/runtime `paused` + `autoContinue=false` and no queued continuations, replay stale checkpoint, confirm stale prompt + abort before work, verify `bash`, `goal_question`, `subagent`, and unknown tool are blocked while `get_goal` remains allowed, then run `/goal-resume` and confirm active `autoContinue=true` with one clean continuation. Output saved to `/tmp/manual-qa-paused-resume-output.json`.
