- 2026-05-25: Created stacked paused-resume guard spec after reproducing stale checkpoint cascade in resumed paused-goal sessions. Explore/research mapped likely fix to startup/tree continuation queue gating and before-agent-start reconciliation before checkpoint actionability.
- 2026-05-25: Implemented paused-resume guard. Startup, tree, and compaction paths now queue only actionable focused continuations; session resume no longer prompts/reactivates paused goals; `before_agent_start` reconciles focused goal from disk before checkpoint actionability so persisted paused state stales/aborts old checkpoints. Added regression coverage for paused `session_start`, `session_tree`, disk-reconciled stale checkpoint abort, stale fallback blocking with safe `get_goal`, and explicit `/goal-resume` active continuation. Focused verification passed: `node --experimental-strip-types --test tests/goal-extension.test.ts`.
