- 2026-05-25: Created stacked paused-resume guard spec after reproducing stale checkpoint cascade in resumed paused-goal sessions. Explore/research mapped likely fix to startup/tree continuation queue gating and before-agent-start reconciliation before checkpoint actionability.
- 2026-05-25: Implemented paused-resume guard. Startup, tree, and compaction paths now queue only actionable focused continuations; session resume no longer prompts/reactivates paused goals; `before_agent_start` reconciles focused goal from disk before checkpoint actionability so persisted paused state stales/aborts old checkpoints. Added regression coverage for paused `session_start`, `session_tree`, disk-reconciled stale checkpoint abort, stale fallback blocking (`bash`, `goal_question`, `subagent`, unknown tool) with safe `get_goal`, and explicit `/goal-resume` active continuation. Verification passed: `node --experimental-strip-types --test tests/goal-extension.test.ts`, `npm test`, `bun test`, `npm run check`, and `git diff --check`.
- 2026-05-25: Ran isolated local extension QA with a temporary session directory via `/tmp/manual-qa-paused-resume.mjs`. Flow: create dummy Sisyphus goal, pause it, emit `session_start` resume and `session_tree`, confirm persisted/runtime `paused` + `autoContinue=false` and no queued continuations, replay stale checkpoint, confirm stale prompt + abort before work, verify `bash`, `goal_question`, `subagent`, and unknown tool are blocked while `get_goal` remains allowed, then run `/goal-resume` and confirm active `autoContinue=true` with one clean continuation. Output saved to `/tmp/manual-qa-paused-resume-output.json`.
- 2026-05-25: Local Pi paused-resume QA passed using patched extension in isolated `/tmp/pi-goal-x-paused-resume2.wtgHIa`: created dummy goal, paused it, verified persisted `status=paused` and `autoContinue=false`, `/goal` inspection exited cleanly, stale checkpoint requesting work returned refusal without executing requested tools, and explicit `/goal-resume` restored `status=active` and `autoContinue=true`.
- 2026-05-26: Fixed resumed active-goal post-stop poisoning. Split neutral runtime clear from explicit post-stop marking: focus/load/resume clears no longer derive `turnStoppedFor`, while pause/clear/abort/replace paths mark stopped explicitly. Added regression proving `/goal-resume` queued continuation allows first `read` before `turn_start` and after. Verification: `node --experimental-strip-types --test tests/goal-extension.test.ts` passed.
- 2026-05-26: Added second guard for stale post-stop poisoning. `turnStoppedFor` is now scoped to a turn generation advanced by turn/session/agent-start hooks; older markers self-clear on `tool_call`. Active `/goal-resume` already-running and `/goal-focus` continuation paths clear stale marker state before arming. Added regressions for already-running resume clearing stale marker and prior-generation marker allowing `fff_multi_grep` without `turn_start`.
- 2026-05-26: Fixed active checkpoint prep-tool poisoning. Non-progress prep/recon tools such as `session_recall` and `fff_multi_grep` no longer mark active auto-continue goals stopped; they remain allowed and only avoid setting `goalWorkToolCalledThisTurn`, preserving empty-turn auto-continue suppression. Added regression for `session_recall` -> `fff_multi_grep` -> `read` in one active checkpoint turn.
